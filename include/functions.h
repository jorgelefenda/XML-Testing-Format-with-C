#ifndef FUNCTIONS_H
#define FUNCTIONS_H

double calcularRetorno(double precio_actual, double precio_anterior);
double calcularMedia(double* datos, int n);
double calcularVolatilidad(double* datos, int n);

#endif